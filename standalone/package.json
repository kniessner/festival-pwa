{
  "name": "festival-pwa-standalone",
  "version": "1.0.0",
  "private": true,
  "description": "Bucht der Träumer Festival Guide — build-less PWA, served as static files.",
  "scripts": {
    "start": "node server.js",
    "serve": "node server.js",
    "build": "node scripts/build.js"
  },
  "engines": {
    "node": ">=14"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "sharp": "^0.35.3"
  }
}
